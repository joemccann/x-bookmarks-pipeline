"""
Trading Engine — Python indicators and strategies powered by x-bookmarks-pipeline signals.

Reads from signals.db (shared SQLite). Completely independent of src/.
Can be extracted into its own repo with zero import changes.
"""
from __future__ import annotations

__version__ = "0.1.0"
